[project]
name = "galileogps"
version = "0.3.0"
description = "Multi-GNSS (Galileo + GPS + GLONASS + BeiDou) layer for the Nexus ecosystem"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Esslinger & Co. / Nexus" }]

[tool.pytest.ini_options]
pythonpath = ["."]
